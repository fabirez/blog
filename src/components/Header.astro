---
import { Icon } from "astro-icon/components";
import ThemeToggle from "./ThemeToggle.astro";

const linksNav = [
  {
    nameLink: "Experiment",
    link: "/experiment/",
  },
];
---

<header class="mx-2 mt-8 flex w-5xl items-center justify-between lg:mx-4">
  <a href="/" class="flex">
    <div class="cont_logo flex items-center">
      <Icon name="caffeine" class="text-foreground" />
      <div class="flex h-8 items-start">
        <Icon name="asterisk-icon" class="asterisk" />
      </div>
    </div>
  </a>

  <div class="flex items-center gap-4">
    <nav>
      <ul class="flex gap-8 list-none!">
        {
          linksNav.map(({ nameLink, link }) => (
            <li class="!mt-0">
              <a
                href={link}
								class="link"
              >
                {nameLink}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <div class="mx-2 h-6 w-px bg-gray-950/10 dark:bg-white/20 rotate-30"></div>
    <ThemeToggle />
  </div>
</header>
